int main()
{
   int year;
   printf("enter the year");
   scanf("%d",&year);
   if (year%4==0)
   {
   printf("%d is leap year", year);	
   }
   else if(year%4!=0)
   {
   printf("%d not a leap year",year);	
   }
   return 0;
}
